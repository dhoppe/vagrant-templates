---
:backends:
  - 'yaml'

:hierarchy:
  - '%{::hostname}/%{calling_module}'
  - '%{::domain}/%{calling_module}'
  - '%{calling_module}'
  - 'common'

:yaml:
  :datadir: '/vagrant/hieradata/%{::environment}'
